name: reminders
description: Get reminders from Apple Reminders
version: 0.0.1
author: Berko Bob
homepage: www.babblevoice.com

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  plugin:
    platforms:
      ios:
        pluginClass: RemindersPlugin
